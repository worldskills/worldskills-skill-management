'use strict';

describe('Controller: AssessmentcriteriamenuCtrl', function () {

  // load the controller's module
  beforeEach(module('worldSkillsAppApp'));

  var AssessmentcriteriamenuCtrl,
    scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller, $rootScope) {
    scope = $rootScope.$new();
    AssessmentcriteriamenuCtrl = $controller('AssessmentcriteriamenuCtrl', {
      $scope: scope
    });
  }));

  
});
